# Sensitive Configuration (API Keys, Passwords)
# These values can override config.yaml settings

# OpenAI Configuration (if using OpenAI embeddings)
OPENAI_API_KEY=your-openai-key

# Override Neo4j credentials if needed (optional)
NEO4J_URI=neo4j_url
NEO4J_USER=username
NEO4J_PASSWORD=password

# Override log level if needed (optional)
# LOG_LEVEL=INFO
